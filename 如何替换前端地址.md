# 如何替换前端地址 - 详细步骤

## 📍 需要替换的两个地方

部署时需要配置两个地址：

1. **后端环境变量 `FRONTEND_URL`**：告诉后端允许哪个前端地址访问（CORS配置）
2. **前端环境变量 `VITE_API_BASE_URL`**：告诉前端后端API的地址

---

## 🔍 第一步：获取您的前端 Vercel 地址

### 方法1：在 Vercel 控制台查看

1. 访问 https://vercel.com
2. 登录您的账号
3. 点击您的项目（`gaozhiliang`）
4. 在项目页面，您会看到部署信息
5. **前端地址**通常在页面顶部，格式类似：
   ```
   https://gaozhiliang.vercel.app
   ```
   或
   ```
   https://gaozhiliang-xxx.vercel.app
   ```

### 方法2：查看部署历史

1. 在 Vercel 项目页面
2. 点击 **"Deployments"** 标签
3. 点击最新的部署记录
4. 在部署详情中可以看到 **"Domains"**（域名）
5. 复制这个地址（通常是 `https://xxx.vercel.app`）

### 方法3：如果您设置了自定义域名

如果您在 Vercel 中设置了自定义域名，使用自定义域名即可。

---

## 🔧 第二步：在 Render 中设置 FRONTEND_URL

### 步骤1：进入 Render 服务设置

1. 登录 Render：https://render.com
2. 进入您的服务（`gaozhiliang-backend`）
3. 点击 **"Environment"** 标签

### 步骤2：添加或修改 FRONTEND_URL

**如果还没有添加 `FRONTEND_URL`：**
1. 点击 **"Add Environment Variable"**
2. **Key**: 输入 `FRONTEND_URL`
3. **Value**: 输入您的前端地址（例如：`https://gaozhiliang.vercel.app`）
   - ⚠️ **重要**：必须包含 `https://` 协议
   - ⚠️ **不要**在末尾加 `/`
4. 点击 **"Save Changes"**

**如果已经添加了 `FRONTEND_URL`：**
1. 找到 `FRONTEND_URL` 这一行
2. 点击右侧的 **"Edit"** 或 **"Update"** 按钮
3. 修改 **Value** 为您的实际前端地址
4. 点击 **"Save Changes"**

### 步骤3：重新部署（如果需要）

修改环境变量后，Render 可能需要重新部署：
- 有些情况下会自动重新部署
- 如果没有自动部署，点击 **"Manual Deploy"** -> **"Deploy latest commit"**

---

## 🔧 第三步：在 Vercel 中设置 VITE_API_BASE_URL

### 步骤1：获取后端 Render 地址

1. 在 Render 服务页面
2. 页面顶部会显示服务的 URL，格式类似：
   ```
   https://gaozhiliang-backend.onrender.com
   ```
3. 复制这个地址

### 步骤2：在 Vercel 中添加环境变量

1. 登录 Vercel：https://vercel.com
2. 进入您的项目（`gaozhiliang`）
3. 点击 **"Settings"**（设置）
4. 在左侧菜单中，点击 **"Environment Variables"**（环境变量）

### 步骤3：添加 VITE_API_BASE_URL

1. 点击 **"Add New"** 或 **"Add Environment Variable"**
2. 填写信息：
   - **Name**: `VITE_API_BASE_URL`
   - **Value**: `https://gaozhiliang-backend.onrender.com/api`
     - ⚠️ **注意**：需要在后端地址后面加上 `/api`
     - 例如：如果后端地址是 `https://gaozhiliang-backend.onrender.com`
     - 那么这里应该填：`https://gaozhiliang-backend.onrender.com/api`
   - **Environment**: 
     - ✅ 勾选 `Production`（生产环境）
     - ✅ 勾选 `Preview`（预览环境）
     - ✅ 勾选 `Development`（开发环境）
     - 建议全选，这样所有环境都能正常工作
3. 点击 **"Save"**

### 步骤4：重新部署前端

添加环境变量后，Vercel 会自动触发重新部署：
1. 在项目页面，您会看到新的部署任务
2. 等待部署完成（通常1-2分钟）
3. 部署完成后，前端就会使用新的后端地址了

**如果没有自动部署**：
1. 点击 **"Deployments"** 标签
2. 点击最新的部署记录右侧的 **"..."** 菜单
3. 选择 **"Redeploy"**

---

## ✅ 第四步：验证配置

### 验证后端配置

1. 访问后端健康检查：
   ```
   https://gaozhiliang-backend.onrender.com/health
   ```
2. 应该返回：
   ```json
   {
     "status": "ok",
     "timestamp": "2024-..."
   }
   ```

### 验证前端配置

1. 访问您的前端地址（例如：`https://gaozhiliang.vercel.app`）
2. 打开浏览器开发者工具（按 `F12`）
3. 切换到 **"Network"**（网络）标签
4. 尝试登录或使用任何功能
5. 查看网络请求，应该能看到请求发送到：
   ```
   https://gaozhiliang-backend.onrender.com/api/...
   ```

### 验证 CORS 配置

如果前端能正常调用后端API，说明 CORS 配置正确。如果遇到 CORS 错误：
1. 检查 Render 中的 `FRONTEND_URL` 是否正确
2. 确保包含 `https://` 协议
3. 确保没有多余的斜杠 `/`

---

## 📝 配置示例

### 假设您的前端地址是：
```
https://gaozhiliang.vercel.app
```

### 假设您的后端地址是：
```
https://gaozhiliang-backend.onrender.com
```

### 那么在 Render 中设置：
```
FRONTEND_URL = https://gaozhiliang.vercel.app
```

### 在 Vercel 中设置：
```
VITE_API_BASE_URL = https://gaozhiliang-backend.onrender.com/api
```

---

## 🐛 常见问题

### Q: 我不知道前端地址是什么？
A: 
- 登录 Vercel，在项目页面就能看到
- 或者查看部署历史中的域名信息

### Q: 修改环境变量后需要重新部署吗？
A: 
- **Render**：修改环境变量后通常需要手动重新部署
- **Vercel**：添加环境变量后会自动重新部署

### Q: 前端还是无法连接后端？
A: 检查以下几点：
1. 后端地址是否正确（包含 `https://`）
2. 前端环境变量 `VITE_API_BASE_URL` 是否正确（包含 `/api`）
3. 后端 `FRONTEND_URL` 是否与前端地址完全一致
4. 查看浏览器控制台和网络请求，看具体错误信息

### Q: 出现 CORS 错误？
A: 
- 检查 Render 中的 `FRONTEND_URL` 是否与前端地址完全一致
- 确保包含协议（`https://`）
- 确保没有多余的斜杠

---

## 🎉 完成！

配置完成后，您的前后端就能正常通信了！

