# 快速推送代码指南

## 🎯 最简单的方法

### 步骤1：创建 GitHub Personal Access Token

1. **访问 Token 创建页面**：
   ```
   https://github.com/settings/tokens/new
   ```
   或者：
   - 登录 GitHub
   - 点击右上角头像 -> Settings
   - 左侧菜单：Developer settings -> Personal access tokens -> Tokens (classic)
   - 点击 "Generate new token" -> "Generate new token (classic)"

2. **填写信息**：
   - **Note**：`Git推送`（随便填）
   - **Expiration**：选择 `90 days` 或 `No expiration`
   - **Select scopes**：✅ 勾选 `repo`（完整仓库访问权限）

3. **生成 Token**：
   - 滚动到底部，点击 "Generate token"
   - ⚠️ **立即复制 Token**（格式：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）
   - Token 只会显示一次！

### 步骤2：使用脚本推送（最简单）

运行以下命令，然后按提示输入 Token：

```bash
cd /Users/choo/gaozhiliang
./git-push-helper.sh
```

### 步骤3：手动配置（如果脚本不行）

如果您已经创建了 Token，可以手动配置：

```bash
# 替换 YOUR_TOKEN 为您的实际 Token
cd /Users/choo/gaozhiliang
git remote set-url origin https://YOUR_TOKEN@github.com/HicentChoo/gaozhiliang.git
git push origin main
```

**示例**（假设 Token 是 `ghp_abc123def456`）：
```bash
git remote set-url origin https://ghp_abc123def456@github.com/HicentChoo/gaozhiliang.git
git push origin main
```

---

## 🔒 安全提示

- Token 相当于密码，请妥善保管
- 不要将 Token 提交到代码仓库
- 如果 Token 泄露，立即在 GitHub 上删除并重新创建

---

## ✅ 推送成功后

推送成功后，在 Render 中：
1. 进入您的服务页面
2. 点击 "Manual Deploy" -> "Deploy latest commit"
3. 等待构建完成

